steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['pull', 'gcr.io/$PROJECT_ID/atlas-frontend']
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '--cache-from', 'gcr.io/$PROJECT_ID/atlas-frontend:latest',
    '--build-arg', 'BUILD_REVISION=$COMMIT_SHA',
    '-t', 'gcr.io/$PROJECT_ID/atlas-frontend:$COMMIT_SHA',
    '-t', 'gcr.io/$PROJECT_ID/atlas-frontend:latest',
    '.'
  ]
images:
- 'gcr.io/$PROJECT_ID/atlas-frontend:$COMMIT_SHA'
- 'gcr.io/$PROJECT_ID/atlas-frontend:latest'
